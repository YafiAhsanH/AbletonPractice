import React from "react";

const FooterSectionTitle = ({ children }: { children: React.ReactNode }) => {
  return <div className="font-semibold leading-8">{children}</div>;
};

export default FooterSectionTitle;
